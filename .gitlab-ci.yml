.job-template: &gravedigger
  image: python:3.6-alpine
  before_script:
    - pipenv install
  script:
    - pipenv run python gravedigger/gravedigger.py
  artifacts:
    paths:
      - gravedigger.log
    expire_in: 1 week

# -----------------------------------------------------------------------------

clean_10c:
  <<: *gravedigger
  tags:
    - 10c
